
#hola amigos prueba de audio
para imprimir o alimentar los datos de los arreglos, usamos 

        for(i = 0;i<4 ; i++ )

 esto repetira el proceso hasta que i sea mayor que 4.
 
        for(i = 0;i<4 ; i++ )
        {
            printf("\n\n%f",temp[i]);
        }
en arreglos 4 definimos una variable N que modifica el valor del arreglo a el gusto del usuario, despues de que el usuario le de el valor a N, 
el valor de N se ajusta a el tamaño del arreglo, a la cantidad de veces que se repite el for para darles valor a el arreglo y para imprimir todos los valores del arreglo.
